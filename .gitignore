# dist is now committed directly so Vercel serves pre-built files
# dist/

# Dependencies
node_modules/

# Environment / secrets (NUNCA commitar)
.env
.env.local
firebase-config.local.js

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Claude settings (may contain secrets)
.claude/settings.local.json

# npm lock (not needed for this project)
package-lock.json

# Python cache (servidor local)
__pycache__/
*.pyc
.claude/
.claude/settings.local.json
icone_b64.txt
icone_b64_str.txt
supabase/.temp/
